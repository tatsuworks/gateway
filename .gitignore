vendor
rawetf
hmm.js
node_modules
cmd/state/state
cmd/gateway/gateway
cmd/gatewayc/gatewayc
cmd/forceidentify/forceidentify
cmd/requestmembers/requestmembers
cmd/gateway/token.go
dump.rdb
logs.txt
build/bin
docker-secret.yaml
target/

# Per-branch handoffs are ephemeral working state, not history.
# Real handoffs stay local; the _template.md seed is committed so the
# convention travels with the repo.
handoffs/*
!handoffs/_template.md
